<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
      xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/android_refresh"
        android:title="@string/action_refresh_name"
        app:showAsAction="always"/>

    <!-- Settings, should always be in the overflow -->
    <item
        android:id="@+id/action_ict"
        android:icon="@mipmap/ic_web_asset_white_24dp"
        android:title="ICT"
        app:showAsAction="always"/>

    <item
        android:icon="@mipmap/ic_more_vert_white_24dp"
        android:title="Options"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/action_profile"
                android:icon="@drawable/android_profile"
                android:title="@string/action_profile_name"/>

            <item
                android:id="@+id/action_roosters"
                android:icon="@android:drawable/ic_menu_my_calendar"
                android:title="Roosters"
                />

            <item
                android:id="@+id/settings"
                android:icon="@mipmap/android_settings"
                android:title="@string/action_settings_name"
                app:showAsAction="never"/>
        </menu>
    </item>

</menu>